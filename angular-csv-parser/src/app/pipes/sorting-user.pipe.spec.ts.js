import { SortingCompaniesPipe } from './sorting-users.pipe';

describe('SortingCompaniesPipe', () => {
  it('create an instance', () => {
  const pipe = new SortingCompaniesPipe();
  expect(pipe).toBeTruthy();
});
});
